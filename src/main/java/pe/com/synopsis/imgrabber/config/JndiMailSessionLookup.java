package pe.com.synopsis.imgrabber.config;

public class JndiMailSessionLookup{/* extends JndiLocatorSupport {
	  public JndiMailSessionLookup() {
	    setResourceRef(true);
	  }

	 
	  public Session getDataSource(String dataSourceName) throws DataSourceLookupFailureException {
	    try {
	      return lookup(dataSourceName, Session.class);
	    } catch (NamingException ex) {
	      throw new DataSourceLookupFailureException(
	          "Failed to look up JNDI DataSource with name '" + dataSourceName + "'", ex);
	    }
	  }*/
	}